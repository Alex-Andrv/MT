import grammar.gen.ParserGrammarLexer;
import grammar.gen.ParserGrammarParser;
import org.antlr.v4.runtime.CharStreams;
import org.antlr.v4.runtime.CommonTokenStream;

import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Path;

public class Main {
    public static void main(String[] args) throws IOException {
//        Path outDir;
//        System.out.println("Create outDir");
//        Files.createDirectories(outDir);
//        ParserGrammarLexer lexer = new ParserGrammarLexer(CharStreams.fromStream(System.in));
//        ParserGrammarParser parser = new ParserGrammarParser(new CommonTokenStream(lexer));
//        parser.lexerRules();
    }
}
